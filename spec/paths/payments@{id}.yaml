get:
  tags:
    - Payments
  summary: Get payment details
  description: |
    Returns the details of the payment with the specified identifier string.

    If the payment method requires a redirection to a third party (e.g., 3D Secure),
    the redirect URL back to your site will include a `cko-session-id` query parameter
    containing a payment session ID that can be used to obtain the details of the payment, for example:

    http://example.com/success?cko-session-id=sid_ubfj2q76miwundwlk72vxt2i7q.
  parameters:
    - in: path
      name: id
      schema:
        type: string
        pattern: "^(pay|sid)_(\\w{26})$"
      required: true
      description: The payment or payment session identifier
  responses:
    '200':
      description: Payment retrieved successfully
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Payment'
      headers:
        Cko-Request-Id:
          $ref: "#/components/headers/Cko-Request-Id"
        Cko-Version:
          $ref: "#/components/headers/Cko-Version"
    '401':
      description: Unauthorized
    '404':
      description: Payment not found

# put:
#   tags:
#     - Payments
#   summary: Update payment details
#   description: |
#     Updates the details of the payment with the specified identifier string.
#     Currently only the `amount` can be updated for pending 3DS payments.
#   parameters:
#     - in: path
#       name: id
#       schema:
#         type: string
#         pattern: "^(pay|sid)_(\\w{26})$"
#       required: true
#       description: The payment or payment session identifier
#   requestBody:
#     content:
#       application/json:
#         schema:
#           type: object
#           properties:
#             amount:
#               type: integer
#               description: |
#                 The payment amount in the major currency.
#                 Omit the amount or provide a value of 0 to perform a card verification
#               minimum: 0
#               example: 6540
#   responses:
#     '200':
#       description: Payment updated successfully
#       content:
#         application/json:
#           schema:
#             $ref: '#/components/schemas/Payment'
#       headers:
#         Cko-Request-Id:
#           $ref: "#/components/headers/Cko-Request-Id"
#         Cko-Version:
#           $ref: "#/components/headers/Cko-Version"
#     '401':
#       description: Unauthorized
#     '422':
#       description: Invalid data was sent
#       content:
#         application/json:
#           schema:
#             $ref: '#/components/schemas/ValidationError'
#     '403':
#       description: Unable to update payment
#       content:
#         application/json:
#           schema:
#             $ref: '#/components/schemas/ErrorResponse'
#     '404':
#       description: Payment not found